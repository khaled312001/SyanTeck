# دليل التحويل إلى منصة صيانة تك

## ✅ ما تم إنجازه

### 1. قاعدة البيانات
- ✅ إضافة حقول جديدة لجدول `orders`
- ✅ إنشاء جداول جديدة: `regions`, `pricing_rules`, `quality_followups`, `technician_regions`
- ✅ تحديث جدول `users` بإضافة حقول الفنيين

### 2. النماذج (Models)
- ✅ `Region.php` - إدارة المناطق
- ✅ `PricingRule.php` - قواعد التسعير
- ✅ `QualityFollowup.php` - متابعات الجودة
- ✅ تحديث `Order.php` و `User.php` بالعلاقات الجديدة

### 3. المتحكمات (Controllers)
- ✅ `SupportController.php` - لوحة تحكم الدعم
- ✅ `TechnicianController.php` - لوحة تحكم الفني
- ✅ `ClientController.php` - لوحة تحكم العميل
- ✅ `QualityController.php` - لوحة تحكم الجودة
- ✅ `FinanceController.php` - لوحة تحكم المالية
- ✅ `QRController.php` - طلبات QR
- ✅ `TrackingController.php` - تتبع الطلبات

### 4. المسارات (Routes)
- ✅ `routes/support.php`
- ✅ `routes/technician.php`
- ✅ `routes/client.php`
- ✅ `routes/quality.php`
- ✅ `routes/finance.php`
- ✅ تحديث `routes/web.php` بإضافة المسارات العامة

### 5. Middleware
- ✅ `TechnicianCheck.php`
- ✅ `SupportCheck.php`
- ✅ `QualityCheck.php`
- ✅ `FinanceCheck.php`
- ✅ `ClientCheck.php`
- ✅ تسجيلها في `Kernel.php`

### 6. Helpers
- ✅ `MaintenanceHelpers.php` - دوال مساعدة للصيانة

### 7. Seeders
- ✅ `MaintenanceRolesSeeder.php` - إنشاء الأدوار والصلاحيات

---

## 📋 خطوات التنفيذ

### الخطوة 1: تشغيل الهجرات

```bash
cd @core
php artisan migrate
```

### الخطوة 2: تشغيل Seeder للأدوار

```bash
php artisan db:seed --class=MaintenanceRolesSeeder
```

### الخطوة 3: تحديث ملف helpers.php

أضف السطر التالي في بداية ملف `@core/app/Helpers/helpers.php`:

```php
require_once __DIR__ . '/MaintenanceHelpers.php';
```

أو أضف الدوال مباشرة في `helpers.php`.

### الخطوة 4: تحديث FrontendController

أضف الدوال التالية في `FrontendController.php`:

```php
public function howItWorks()
{
    return view('how-it-works');
}

public function services()
{
    $categories = Category::where('status', 1)->get();
    $services = Service::where('status', 1)->get();
    return view('services', compact('categories', 'services'));
}
```

### الخطوة 5: إنشاء القوالب (Views)

يجب إنشاء القوالب التالية:

#### لوحة تحكم الدعم:
- `resources/views/support/dashboard.blade.php`
- `resources/views/support/orders/index.blade.php`
- `resources/views/support/orders/show.blade.php`

#### لوحة تحكم الفني:
- `resources/views/technician/dashboard.blade.php`
- `resources/views/technician/orders/index.blade.php`
- `resources/views/technician/orders/show.blade.php`
- `resources/views/technician/profile.blade.php`

#### لوحة تحكم العميل:
- `resources/views/client/dashboard.blade.php`
- `resources/views/client/orders/index.blade.php`
- `resources/views/client/orders/create.blade.php`
- `resources/views/client/track.blade.php`
- `resources/views/client/invoice.blade.php`
- `resources/views/client/warranty.blade.php`

#### لوحة تحكم الجودة:
- `resources/views/quality/followups/index.blade.php`
- `resources/views/quality/followups/create.blade.php`
- `resources/views/quality/followups/show.blade.php`

#### لوحة تحكم المالية:
- `resources/views/finance/invoices/index.blade.php`
- `resources/views/finance/reports.blade.php`
- `resources/views/finance/statistics.blade.php`

#### صفحات عامة:
- `resources/views/qr/index.blade.php`
- `resources/views/tracking/show.blade.php`
- `resources/views/how-it-works.blade.php`
- `resources/views/services.blade.php`

---

## 🔐 الأدوار والصلاحيات

### الأدوار الجديدة:
1. **Support Agent** - موظف الدعم
2. **Technician** - الفني
3. **Client** - العميل
4. **Quality Agent** - موظف الجودة
5. **Finance** - المالية

### تعيين الأدوار للمستخدمين:

```php
use App\User;
use Spatie\Permission\Models\Role;

$user = User::find(1);
$role = Role::where('name', 'Technician')->first();
$user->assignRole($role);
```

---

## 📝 ملاحظات مهمة

### 1. التوافق مع النظام القديم
- تم الحفاظ على `seller_id` و `buyer_id` في جدول `orders`
- تم إضافة علاقات `technician()` و `client()` كـ aliases
- النظام القديم سيعمل بشكل طبيعي

### 2. رموز التتبع والضمان
- يتم إنشاء رموز تتبع تلقائياً عند إنشاء الطلب
- يتم إنشاء رموز ضمان عند إكمال الطلب
- الصيغة: `TRK` + 8 أحرف عشوائية للتتبع
- الصيغة: `WRT` + 8 أحرف عشوائية للضمان

### 3. تعيين الفنيين
- يمكن تعيين الفنيين يدوياً من لوحة الدعم
- يمكن تعيينهم تلقائياً حسب المنطقة والتخصص
- الفني يمكنه قبول أو رفض الطلب

### 4. حالات الطلبات
- `0` - Pending (قيد الانتظار)
- `1` - Active (نشط)
- `2` - Completed (مكتمل)
- `3` - Delivered (تم التسليم)
- `4` - Cancelled (ملغي)

### 5. حالات الفني
- `en_route` - في الطريق
- `arrived` - وصل
- `started` - بدء العمل
- `completed` - اكتمال العمل

---

## 🚀 المميزات الجديدة

### 1. نظام QR للطلبات
- يمكن للعملاء إنشاء طلبات بدون تسجيل دخول
- يتم إنشاء رمز تتبع فريد لكل طلب
- يمكن تتبع الطلب برقم التتبع

### 2. نظام التتبع
- تتبع مباشر لحالة الطلب
- عرض تفاصيل الطلب والمواعيد
- متاح للجميع بدون تسجيل دخول

### 3. نظام الفواتير
- إنشاء فواتير تلقائية
- تصدير PDF (يحتاج تحديث InvoiceController)
- تتبع حالة الدفع

### 4. نظام الضمانات
- ضمانات إلكترونية لكل طلب مكتمل
- تصدير PDF (يحتاج تحديث WarrantyController)
- تتبع فترة الضمان

### 5. نظام متابعة الجودة
- تقييم جودة الخدمة
- ملاحظات من العميل والفني
- تحديث تقييم الفني تلقائياً

### 6. نظام التسعير الذكي
- قواعد تسعير قابلة للتخصيص
- تسعير حسب الفئة/الخدمة/المنطقة/الأولوية
- رسوم إضافية (نسبة مئوية أو مبلغ ثابت)

---

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى مراجعة:
1. ملف `خطة_التنفيذ_المتبقية.md`
2. ملفات الكود المصدري
3. توثيق Laravel و Spatie Permission

---

## ⚠️ تحذيرات

1. **النسخ الاحتياطي**: تأكد من عمل نسخة احتياطية من قاعدة البيانات قبل تشغيل الهجرات
2. **الاختبار**: اختبر جميع الوظائف في بيئة التطوير أولاً
3. **الأدوار**: تأكد من تعيين الأدوار الصحيحة للمستخدمين
4. **الصلاحيات**: راجع الصلاحيات في `MaintenanceRolesSeeder.php`

---

## 📅 التحديثات المستقبلية

- [ ] إضافة نظام الإشعارات
- [ ] إضافة نظام الدردشة
- [ ] تحسين نظام تعيين الفنيين
- [ ] إضافة تقارير متقدمة
- [ ] إضافة تطبيق موبايل
- [ ] إضافة نظام التقييمات

---

**تم الإنشاء في:** {{ date('Y-m-d') }}
**الإصدار:** 1.0.0

